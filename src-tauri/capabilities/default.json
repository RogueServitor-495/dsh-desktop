{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for app windows (control panel, main DSH window, approval popup)",
  "windows": [
    "control",
    "gui",
    "approval-popup"
  ],
  "permissions": [
    "core:default"
  ]
}